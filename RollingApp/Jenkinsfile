pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello again, jenkins'
            }
        }
        stage('Build Docker Image') {
            steps {
                dir('RollingApp/RollingApp') {
                    sh 'ls -l'
                    sh 'docker compose build'
                }
            }
        }
        stage('Start Docker Container') {
            steps {
                dir('RollingApp/RollingApp') {
                    sh 'docker compose up -d'
                }
            }
        }
    }
}
